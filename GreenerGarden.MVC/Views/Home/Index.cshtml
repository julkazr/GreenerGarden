@model  HomeViewModel

 @{
    ViewData["Title"]="Home Page" ;
}

<div class="container">
    <button class="btn green-bckg mt-5"> @Html.ActionLink("Create New Season", "Create", "Season", null, new { @class = "text-warning h4" })</button>
    <hr />
    <h4>Current season </h4>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Culture</th>
                <th>Expences</th>
                <th>Yield</th>
                <th>Profit</th>
            </tr>
        </thead>
        <tbody>
            @*@foreach (var item in Model.CultureNames)
            {*@
            <tr>
                @*<td>@Html.DisplayFor(modelItem => item.)</td>
        <td>@Html.DisplayFor(modelItem => item.Expences)</td>
        <td>@Html.DisplayFor(modelItem => item.CropYield)</td>
        <td>@Html.DisplayFor(modelItem => item.Profit)</td>*@
                <td>Letuce</td>
                <td>60.00</td>
                <td>20</td>
                <td>540.00</td>
            </tr>
            @*}*@
        </tbody>
    </table>
    <div class="row">
        <p class="col-6 bigger-font">Total expences in current season:</p>
        <p class="col-6 bigger-font">@Html.DisplayFor(model => model.TotalExpence)</p>
    </div>
    <div class="row">
        <p class="col-6 bigger-font">Total profit in current season: </p>
        <p class="col-6 bigger-font">@Html.DisplayFor(model => model.TotalProfit)</p>
    </div>
    <hr />
    <button class="btn green-bckg mt-5"> @Html.ActionLink("Delete Season", "Delete", "Season", null, new { @class = "text-warning h4" })</button>
</div>
